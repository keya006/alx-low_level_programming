#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point of program
 * @argc: param
 * @argv: the string
 *
 * Return: 0 signifies success
 */

int main(int argc, char *argv[])
{
	printf("%d\n", (argc - 1));
	return (0);
}
